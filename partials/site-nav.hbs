<nav class="site-nav">
    <div class="site-nav-left header-navigation">
      {{#if @blog.navigation}}
          {{navigation}}
      {{/if}}

    </div>
    <div class="site-nav-right">
      {{#if @blog.logo}}
          <a class="" href="{{@blog.url}}"><img class="site-logo" src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>
      {{else}}
          <a class="" href="{{@blog.url}}">{{@blog.title}}</a>
      {{/if}}
    </div>
</nav>
